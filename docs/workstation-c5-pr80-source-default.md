# Workstation C5 PR #80 source default

PR #80 added static browser event client coverage for source defaulting during payload normalization.

The guard keeps the event payload helper explicit about local/manual event provenance before any UI wiring is attempted.
